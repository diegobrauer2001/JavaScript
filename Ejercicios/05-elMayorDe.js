//Comparación de números: Escribe una función que reciba dos números y devuelva el mayor de los dos.

function elMayorDe(a,b){
    if(a > b ){
        return (a);
    } else{
        return(b);
    }
}

console.log(elMayorDe(3034,80));
